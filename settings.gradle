pluginManagement {
    repositories {
        jcenter()
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net/'
        }
        gradlePluginPortal()
    }
}

// TODO: Publish API and Loader on some maven repo and reference it from there
// TODO: this is development only

includeBuild("../api") {
    dependencySubstitution {
        substitute module('club.issizler.okyanus:api') with project(':')
    }
}

includeBuild("../loader") {
    dependencySubstitution {
        substitute module('club.issizler.okyanus:loader') with project(':')
    }
}